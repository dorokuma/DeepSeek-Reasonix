
## note #1 · 2026-06-30T07:22:48.121157152Z · kind=summary

# 审计报告: max_steps/MaxSubagentSteps 移除深度审计

## 审计范围
审查 `/root/reasonix` 项目中移除 `TaskTool.maxSteps`/`TaskTool.maxSubagentSteps` 字段、Schema JSON 的 `max_steps` 参数、`Options.MaxSubagentSteps`、`Agent.maxSubagentSteps`、`AgentConfig.MaxSubagentSteps` 以及相关的 `NewTaskTool` 签名变更是否彻底、安全。

## 1. 搜索残留

### 已完全移除（零残留）
| 搜索项 | 匹配数 | 结论 |
|--------|--------|------|
| `MaxSubagentSteps` | 0 | ✅ 完全清除 |
| `maxSubagentSteps` | 0 | ✅ 完全清除 |
| `max_subagent_steps` | 0 | ✅ 完全清除（含文档、TOML 示例、config 默认值） |

### 仍存在但无害的引用（与本次移除无关）
| 搜索项 | 位置 | 说明 |
|--------|------|------|
| `agent.max_steps` / `AgentConfig.MaxSteps` / `Options.MaxSteps` | config.go:473, agent.go:462, boot.go:396-398, acp.go:141, cli.go 多处 | 父 agent 自身的循环步数限制，**非**子 agent/任务工具的 max_steps，应保留 |
| `planner_max_steps` / `MaxStepsKey` | agent.go:558, boot.go:699, coordinator_test.go:196,243 | 规划器独立的只读调用限制，与本次移除无关 |
| `max_steps` TOML 键在文档/示例中 | README.md, GUIDE.md, SPEC.md, reasonix.example.toml | 描述父 agent 的配置参数，不是 task 工具的 JSON 参数 |
| `maxSteps` 变量在 boot.go:396、acp.go:141、cli.go 多处 | boot.go:396-398, cli.go:207,225 等 | 父 agent 的 MaxSteps 传参变量 |
| `maxSteps` 在 task.go:186,236,250,333,353 | task.go | 本地变量，硬编码为 0（unlimited），用于子 agent 的 `Options.MaxSteps`，是子 agent 自身的循环步数（现在设为无限制），**非**暴露给父模型的 JSON 参数 |
| `cmd/e2ebench` 中的 `max_steps`/`maxSteps` | e2ebench/main.go:24,71, diff.go:17 | e2ebench 基准测试工具的配置，非 agent 逻辑 |
| `benchmarks/e2e/tasks/*/task.toml` 中的 `max_steps` | 多个文件 | 基准测试任务的定义，非 agent 配置 |

**结论**: 所有 `max_steps`/`MaxSteps` 残留引用均为与该移除无关的独立概念，不应清理。

## 2. 搜索引用断裂

### 检查项
| 检查项 | 结果 |
|--------|------|
| `NewTaskTool` 的其他调用方 | ✅ 仅 boot.go:493 和 acp.go:146 两处，均已更新为新签名（移除 `maxSteps, maxSubagentSteps` 参数） |
| `Options.MaxSubagentSteps` 的其他读取方 | ✅ 零匹配，已完全移除 |
| `Agent.maxSubagentSteps` 的其他使用方 | ✅ 零匹配，已完全移除 |
| `AgentConfig.MaxSubagentSteps` 的其他读取方 | ✅ 零匹配，已完全移除 |
| Schema JSON 中的 `max_steps` 参数 | ✅ 已从 Schema() 方法中移除 |
| Execute 中的 `MaxSteps` JSON 解析 | ✅ 已从 `var p struct` 中移除，`maxSteps` 硬编码为 0 |

### 其他检查
- `RunSubAgent` 调用方 4 处（boot.go:562, cli/review.go:91, task.go:352, task.go:372 定义）：均不受影响
- `task` 工具的 `runSub` 方法签名（接受 `maxSteps int` 参数）：该参数现在始终为 0，参数名保留但语义正确

**结论**: 无引用断裂。

## 3. 编译验证

| 命令 | 结果 |
|------|------|
| `go build ./...` | ✅ 成功，无错误 |
| `go vet ./...` | ✅ 成功，无警告 |

## 4. 测试运行

| 命令 | 结果 |
|------|------|
| `go test ./...` | ✅ 全部通过（约 50 个包，无 FAIL） |

## 总体结论

**本次 `max_steps`/`MaxSubagentSteps` 移除是彻底的、安全的。**

- 所有应该移除的字段、参数、JSON schema 属性均已从代码中删除
- 两个调用方（boot.go, acp.go）均已同步更新
- 没有遗留的引用或断裂的符号
- 编译和所有测试均通过
- 文档和示例中涉及 `max_subagent_steps` 的引用也已清除
# resolveToolNames 调查报告

## 1. 搜索 resolveToolNames 的定义和所有调用位置

**文件**: `/root/reasonix/internal/permission/permission.go`

- **定义**: 第 138 行 `func resolveToolNames(name string) []string`
- **调用**: 第 165 行 `concreteNames := resolveToolNames(toolName)`（在 `Decide` 方法内）

## 2. resolveToolNames 完整实现代码

```go
// resolveToolNames expands a tool or category name into the set of concrete
// tool names it matches. A bare tool name returns itself; a category name
// returns all tools in that category.
func resolveToolNames(name string) []string {
	if tools, ok := toolCategories[name]; ok {
		return tools
	}
	return []string{name}
}
```

对应的 `toolCategories` 映射（第 128-133 行）：

```go
// toolCategories maps a category name to the set of concrete tool names it
// covers. A permission rule can use a category name (e.g. "Edit") in place of a
// concrete tool name so that one rule can gate every tool in the category.
var toolCategories = map[string][]string{
	"Edit": {"move_file", "write_file"},
}
```

## 3. 传入 "bash" 时的返回值

因为 `"bash"` 不在 `toolCategories` 中，`resolveToolNames("bash")` 直接返回 `[]string{"bash"}`。

**完整返回列表**: `["bash"]`

## 4. categoriesFor 函数实现及 "bash" 的归属

```go
// categoriesFor returns all category names that include the given tool name.
func categoriesFor(toolName string) []string {
	var cats []string
	for cat, tools := range toolCategories {
		for _, t := range tools {
			if t == toolName {
				cats = append(cats, cat)
				break
			}
		}
	}
	return cats
}
```

`toolCategories` 目前只有 `"Edit" -> {"move_file", "write_file"}`。**"bash" 不属于任何 category**，所以 `categoriesFor("bash")` 返回 `nil`。

## 5. 验证 ask 规则匹配问题

在 `Decide` 方法中（第 163-222 行）：

```go
func (p Policy) Decide(toolName string, readOnly bool, args json.RawMessage) Decision {
	concreteNames := resolveToolNames(toolName)       // → ["bash"]
	concreteNames = append(concreteNames, categoriesFor(toolName)...)  // → ["bash"] (无追加)
	...
	for _, name := range concreteNames {   // name = "bash"
		for _, s := range subjects {
			if matchAny(p.Ask, name, s) {  // 匹配 Ask 规则
				return Ask
			}
		}
	}
	...
}
```

`matchAny` 函数（第 226-248 行）：

```go
func matchAny(rules []Rule, toolName, subject string) bool {
	for _, r := range rules {
		if r.Tool != toolName {   // 比较 r.Tool 与每个 concreteNames 中的名字
			continue
		}
		if r.Subject == "" {
			return true
		}
		...
	}
	return false
}
```

**验证结论**：`resolveToolNames("bash")` 返回 `["bash"]`，列表中包含 `"bash"` 本身。因此对于配置中的 ask 规则如 `bash(systemctl *)`，规则解析后 `r.Tool = "bash"`，与 `concreteNames` 中的 `"bash"` 相等，`matchAny` 能正常匹配。**不存在不匹配的问题**。

只有当 `toolCategories` 中有一个名为 `"bash"` 的 category 映射到其他工具名（不含 "bash"）时，`resolveToolNames("bash")` 才可能不返回 `"bash"`。但当前 `toolCategories` 中根本没有 `"bash"` 这个 key，所以不会发生。

## 6. "bash" 工具的注册信息

### (a) 编译期注册 (init)

**文件**: `/root/reasonix/internal/tool/builtin/bash.go`

```go
func init() { tool.RegisterBuiltin(bash{}) }

type bash struct { ... }

func (bash) Name() string { return "bash" }
```

**文件**: `/root/reasonix/internal/tool/tool.go`（RegisterBuiltin 函数）

```go
func RegisterBuiltin(t Tool) {
	builtinsMu.Lock()
	defer builtinsMu.Unlock()
	name := t.Name()      // "bash"
	if _, dup := builtins[name]; dup {
		panic("tool: duplicate built-in " + name)
	}
	builtins[name] = t
}
```

### (b) workspace 绑定

**文件**: `/root/reasonix/internal/tool/builtin/workspace.go`

```go
overrides := map[string]tool.Tool{
    ...
    "bash": bash{workDir: w.Dir, timeout: w.BashTimeout},
    ...
}
```

### (c) 别名映射（用于 Suggest 模糊匹配，不是真正的工具别名）

**文件**: `/root/reasonix/internal/tool/tool.go`

```go
verbMap := map[string]string{
    ...
    "run": "bash", "execute": "bash", "shell": "bash",
    ...
}
```

**注意**：`verbMap` 中的 `"run"`, `"execute"`, `"shell"` 只是 `Suggest` 方法的自然语言模糊匹配映射，它们**不是真正的工具别名**。当模型输入 `"run"` 时，`Suggest` 会建议 `"bash"`。但在 permission 系统中，规则 `run(systemctl *)` 不会被解析为 `bash` 的规则 —— `resolveToolNames` 和 `matchAny` 只处理确切的工具名或 category 展开，不会走 `Suggest` 的 verbMap。

## 总结

`resolveToolNames("bash")` 返回 `["bash"]`，包含自身。permission 系统匹配规则时直接对名字字符串做等值比较，只要规则中 `Tool` 字段是 `"bash"` 就能匹配，不存在丢失问题。
