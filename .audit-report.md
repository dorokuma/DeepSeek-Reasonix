# Audit Report

**Generated:** 2026-06-09T01:10:42Z

## Summary

## Dead Code Analysis Report — 2025-07-17

### Methodology
- `staticcheck -checks=U1000,SA9003,all` 
- `go vet ./...` and `go build ./...`
- Text search for deleted symbols
- Commented-out code block detection

---

### P1 — Empty branch (SA9003), likely a bug
**`internal/agent/agent.go:536-538`**
```go
if hasVisibleAssistantText(text) {
}
```
The conditional checks whether assistant text has visible content, but the body is empty. The code always adds the message at line 539 regardless. This looks like a leftover stub from the recent cleanup. **Fix:** either implement the intended logic or remove the dead conditional.

### P2 — Three unused constants (U1000)
| File | Line | Constant |
|------|------|----------|
| `internal/agent/agent.go` | 32 | `maxFinalReadinessBlocks` |
| `internal/agent/agent.go` | 33 | `maxEmptyFinalBlocks` |
| `internal/tool/builtin/readfile.go` | 34 | `readFileDefaultLimit` |

Each is defined but never referenced. **Fix:** delete or justify in a comment.

### P3 — Style issues
1. **Stale comment** — `internal/agent/testutil/mock_provider.go:140`: doc says "MessageCount" but function is named `CallCount`.
2. **Error string punctuation** — `internal/tool/builtin/audit_finish.go:96`: error ends with "."; omit per Go convention.
3. **Loop style** — `internal/ctxmode/journal.go:216`: can use single `append` with `...` instead of loop.

### ✅ Items fully cleaned (no issues found)
- **`HasWorkSinceLastCheckpoint`** — 0 matches, fully removed
- **`DroppedSinceLastTodo`** — 0 matches, fully removed
- **`evidence` package import** — 0 matches, fully removed
- **Stale `complete_step`/`todo_write` refs** — only legitimate tool-name handling remains; plan-seeding events excluded as intentional
- **`go vet ./...`** — clean (no issues)
- **`go build ./...`** — succeeds
- **`go mod tidy`** — passes
- **Commented-out code blocks** — none found beyond intentional doc examples and cgo blocks
- **Unreachable functions** — none found (the ~15 dead functions were already removed)

## note #1 · 2026-06-09T01:49:51.934167098Z · kind=evidence

# Deep Audit Report — Round 3

## Findings Summary

### [P1] 1. Test failures due to removed tool references in workspace_test.go

**Files:**
- `internal/tool/builtin/workspace_test.go:112-122`
- `internal/tool/builtin/workspace_test.go:112-129`

The tests `TestWorkspaceEnabledFilter` and `TestWorkspacePreservesSessionLevelBuiltins` expect `todo_write` and `complete_step` to be in the built-in tool list. Since these tools were deleted, the tests fail:

```
workspace_test.go:114: enabled filter returned 3 tools: [bash read_file wait]
workspace_test.go:129: workspace tools missing "todo_write"; got [glob write_file audit_finish bash...]
```

**Fix:** Remove `"todo_write"` and `"complete_step"` from the test's expected tool lists.

---

### [P1] 2. serve/wire.go missing event kind mappings for MCPSurfaceReady and Retrying

**File:** `internal/serve/wire.go:109-125`

The `kindNames` map and `toWire` switch are missing entries for `event.MCPSurfaceReady` and `event.Retrying`. When these events are serialized for the HTTP/SSE client, the wire event will have an empty `Kind` string (`""`), which breaks the client's ability to identify the event type.

**Fix:** Add entries:
```go
event.MCPSurfaceReady: "mcp_surface_ready",
event.Retrying:        "retrying",
```
And add cases to the `toWire` switch (at least a default fallthrough for `Retrying` which carries `RetryAttempt`/`RetryMax`).

---

### [P1] 3. desktop/wire.go missing MCPSurfaceReady event kind mapping

**File:** `desktop/wire.go:116-132`

The `kindNames` map includes `Retrying` but NOT `MCPSurfaceReady`. Same empty-kind bug on the desktop wire path.

**Fix:** Add `event.MCPSurfaceReady: "mcp_surface_ready"` to the map.

---

### [P2] 4. Orphaned RunMetrics readiness fields (never populated)

**File:** `internal/cli/run_metrics.go:21-29`

Fields `ReadinessChecks`, `ReadinessAllowed`, `ReadinessBlocks`, `ReadinessRecoveries`, `ReadinessErrors`, `ReadinessMissingProjectChecks`, `ReadinessMissingCompleteSteps`, `ReadinessIncompleteTodos`, `ReadinessCommandMismatches` are vestiges of the removed evidence/readiness-check feature. They are declared, serialised to JSON via `json:"readiness_*"`, but never set — always zero in metrics output.

**Also:** `internal/cli/run_metrics_test.go:14-27` writes all these fields to test their JSON roundtrip.

**Fix:** Remove the dead fields from the struct and update the test. (But if the JSON schema is consumed externally, consider zero-value stability before removing.)

---

### [P2] 5. Stale comment referencing removed `complete_step` tool

**File:** `internal/agent/agent.go:189`

```go
// projectChecks are structured project instructions that complete_step can
// verify against same-turn bash receipts after a write-backed completion.
```

The `complete_step` tool was deleted. The comment should say something like "project checks verified after a write-backed completion."

---

### [P2] 6. Stale `complete_step` entries in tool display maps

**File:** `internal/cli/toolcard.go:49,73`

```go
"complete_step": "Step",   // line 49
"complete_step": "summary", // line 73
```

These display-formatter entries are dead code — the tool no longer exists so the maps are never consulted for this key. Harmless but misleading.

---

### [P2] 7. README.md still lists `todo_write` as a built-in tool

**File:** `README.md:289`

```
built-in tools (read_file, write_file, edit_file, multi_edit, bash, ls, glob, grep, web_fetch, task, todo_write, ask)
```

Should remove `todo_write` from the list.

---

### [P2] 8. Stale references to `todo_write`/`complete_step` in chat_tui.go (comments only)

**File:** `internal/cli/chat_tui.go:89,2287,2362,2435,2983,3008,3017,3019,3165`

The TUI still has extensive logic for handling `todo_write` and `complete_step` events in comments and event-switch cases. The event cases are dead code since the model can no longer call these tools — the events will never arrive. However, the controller still emits synthetic `todo_write` events during plan seeding (`controller.go:1894,1909`), so the event-handling code actually IS still live for those synthetic events. The comments referencing the removed tools are stale.

**Assessment:** The event-handling code for `todo_write` in the TUI is still reachable via synthetic events from `seedPlanTodos`/`completePlanTodos`. It's not dead, but comments are misleading.

---

### [P3] 9. Synthetic `todo_write` events emitted for plan seeding (intentional but fragile)

**File:** `internal/control/controller.go:1894,1909`

The controller emits synthetic events with `Name: "todo_write"` to populate the task panel. Since the real `todo_write` tool was removed, these synthetic events are the only way `todo_write` appears. This works because the frontend handles them for UI rendering only. However, if a frontend tries to look up tool metadata for these events, it won't find any.

---

### [P3] 10. guards_test.go tests reference removed tools (test-only, still valid)

**File:** `internal/agent/guards_test.go:165-196`

Tests `TestPartitionToolCallsCompleteStepSerial` and `TestPartitionToolCallsTodoWriteSerial` create fake tools named `complete_step`/`todo_write` and test the partition logic. These tests are still valid — they register their own fakes — but the test names and comments reference removed tools. Low priority.

---

### Items checked and cleared

- **Race conditions in agent.go:** `parallelisable()` calls `r.Get(name)` which uses RWMutex.RLock() — safe. `planMode` atomic.Bool is accessed with `Load()`/`Store()` correctly. No race found.
- **Nil pointer risks:** All `Registry.Get()` results check `ok` before using the returned `Tool`. Type assertions `v.(sessionCostInfo)` at lines 304, 474, 623 are guarded by `v != nil` / `prev != nil` checks; only `sessionCostInfo` values are ever stored via `atomic.Value`. Safe.
- **Unhandled errors:** All error returns from I/O operations and JSON decode/encode are properly checked. No silent discards found.
- **planApprovalTool constant:** `"exit_plan_mode"` is NOT registered as a real tool anywhere — only used as an event identifier on ApprovalRequest events. Correct by design.
- **No test files import `internal/evidence`** or reference `EvidenceFlow`. Clean.
- **No error messages** reference `complete_step` or `todo_write` by name.
