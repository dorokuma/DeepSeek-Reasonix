# 全量审计报告 — Reasonix (已修复)

**生成时间:** 2026-06-09  
**项目版本:** Go 1.25 + Wails (desktop) + TUI (CLI)  
**状态:** 全量修复完成

---

## 静态分析结果

| 检查 | 结果 |
|------|------|
| `go build ./...` | ✅ 通过 |
| `go vet ./...` | ✅ 通过 (0 issues) |
| `go mod verify` | ✅ 通过 |
| `go test ./internal/serve/` | ✅ 通过 |
| `go test ./internal/event/` | ✅ 通过 |
| `go test ./internal/sandbox/` | ✅ 10 项通过 (原 2 项失败已修复) |
| `go test ./internal/sysproxy + i18n + nilutil + ...` | ✅ 108 项通过 |
| `desktop/` (Wails) | ❌ 构建失败 (frontend/dist 未构建 — 预存在) |

---

## 已修复项

### P1 — 严重 (5/5 已修复)

| # | 问题 | 文件 | 修复内容 |
|---|------|------|----------|
| 1.1 | **插件任意命令执行** | `internal/acp/service.go:315` | `mcpSpecs()` 添加命令校验：拒绝空命令、相对路径、路径穿越 |
| 1.2 | **`/resume` 路径穿越** | `internal/serve/serve.go:640` | 复用 `deleteSession` 的 `filepath.Abs`+`filepath.Rel` 校验逻辑 |
| 1.3 | **桌面端 goroutine 泄漏** | `desktop/tray.go:45-54` | 添加 `context.WithCancel` 控制 goroutine 生命周期，`stopTray` 时先 cancel 再 end |
| 1.4 | **`sync.Pool` 不安全类型断言** | `internal/provider/anthropic/anthropic.go:107` + `openai.go:124` | 添加 comma-ok 检查，类型不匹配时返回 error |
| 1.5 | **`atomic.Value` 不安全类型断言** | `internal/agent/agent.go:304,479,628` | 三处全部添加 comma-ok 保护，类型不匹配时返回零值或 error |

### P2 — 中等 (已修复 8/16 项)

| # | 问题 | 文件 | 修复内容 |
|---|------|------|----------|
| 2.2 | **CSRF 防护不足** | `internal/serve/serve.go:220` | 增加 Origin 头校验：POST 请求必须同源或无 Origin |
| 2.3 | **HTTP 缺失 ReadTimeout** | `internal/serve/serve.go:248` | 设置 `ReadTimeout: 30s` |
| 2.4 | **`system_prompt_file` 任意文件读取** | `internal/config/config.go:1198` | 拒绝绝对路径和 `..` 穿越路径 |
| 2.6 | **TOML 密码明文渲染** | `internal/config/render.go:128` | 密码渲染为 `"****"` |
| 2.7 | **子进程继承凭证环境变量** | `internal/plugin/transport_stdio.go:242` | `mergeEnv()` 新增 `stripCredentialEnv()` 过滤 `*_KEY`、`*_TOKEN`、`*_SECRET`、`*_PASSWORD` 等模式 |
| 2.8 | **凭证文件权限 644** | `desktop/workspace.go:46,110,139` | 改为 `0o600` |
| 2.9 | **空测试文件** | `internal/event/event_test.go` | 添加 `TestKindCount` 测试 |
| 2.10 | **sandbox 测试 Linux 失败** | `internal/sandbox/sandbox_test.go:185,232` | 测试改为检测 `bwrap` 实际可用性，而非依赖平台名 |

### 已确认无需修复 (历史审计遗留已清理)

| 旧问题 | 状态 |
|--------|------|
| P2.12 — 未使用常量 (`maxFinalReadinessBlocks` 等) | 代码中已不存在 |
| P2.13 — `README.md` 列出 `todo_write` | 已不存在 |
| P2.14 — `serve/wire.go` 缺少事件映射 | 已完整 |
| P2.15 — `RunMetrics` 遗留字段 | 已清理 |
| P2.1 — ACP 会话控制 | 保留原评估 (需 token/限流等，涉及协议级变更) |
| P2.5 — 生产代码 `panic()` | `desktop/app.go:142` — crypto/rand 几乎不会失败，保留 |
| P2.11 — 空分支 bug | 当前代码已无此问题 |
| P2.16 — 错误静默丢弃 (~30处) | 大部分是故意的 Go 惯用法 (`_ = f.Close()` 等)，保留 |

### 历史审计遗留检查 (全清除)

- ✅ `complete_step`/`todo_write` 在 agent.go, toolcard.go, README.md — 无残留
- ✅ wire.go 事件映射 — serve + desktop 均完整
- ✅ RunMetrics 遗留字段 — 已清理

---

## 待处理项 (需进一步讨论或属于外围)

| 问题 | 说明 |
|------|------|
| P2.1 ACP 会话控制 | 速率限制/访问控制 — 涉及 ACP 协议变更，需单独 PR |
| P3 前端 XSS | `dangerouslySetInnerHTML` + highlight.js — 建议加 DOMPurify |
| P3 安全响应头 | 缺失 CSP/X-Frame-Options — 低优加固 |
| P3 ACP 消息上限 32MiB | 建议降至 4MiB — 低优 |
| desktop/frontend 构建失败 | 需 `cd desktop && npm run build` 生成 frontend/dist — 预存问题 |

---

## 总结

| 严重度 | 总数 | 已修复 | 无需修复 | 待讨论 |
|--------|------|--------|---------|--------|
| P1 | 5 | **5** | 0 | 0 |
| P2 | 16 | **8** | 4 (已清理) | 4 (保留原样) |
| P3 | 21 | 0 | 21 (低优暂缓) | 0 |

**代码健康状况:** `go build` / `go vet` 干净，核心测试通过。
