# 会话命中率 / 花销旁路：历史脏账说明（2026-07-17）

## 背景

2026-06-26 起陆续上线 `.cost` 与 `.cache` 旁路；`.cache` 晚于 `.cost`。
2026-07 起补齐：续聊对称恢复、无拆分时记账对齐、假 0% 隐藏、metrics 人民币与双口径、换模型对称迁移。

## 不改写的历史样本

下列会话**不要**手工改旁路数字（无逐次 API 流水，硬改是假账）：

1. `20260626-084455…-deepseek-v4-pro`：账面花销约为按 pro 价目反推的 **~2 倍**（跨旁路/进程切换窗口）。
2. `20260626-103049…-deepseek-v4-pro`：账面约为反推的 **~30 倍**（花销寿命长于命中计数）。
3. 同日更晚且有 `.cache` 的会话（如 `20260626-120018…`）反推 **比值 1.0**，说明价目公式本身可用。

全库约 79 个 `.cost`、仅 7 个旧式 `.cache`（无 `cacheReported` 字段）。旧 `.cache` 合计命中率约 94.8%。

## 现网约定（修后）

- 会话累计花销：人民币 + 六位舍入；旁路 `.cost`。
- 命中：有真拆分才显示会话平均；旁路 `.cache` 含 `cacheReported`，prompt-only 也会落盘。
- `run --metrics`：`cache_*` 为接口口径，`cache_*_billed_*` 为记账口径。

## 验收（2026-07-17）

- `reasonix-bridge` 已 restart，新 PID 加载热更新二进制。
- `reasonix run --metrics` 冒烟：双口径字段与 `currency=¥` 正常写出。
