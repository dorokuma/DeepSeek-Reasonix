
## note #4 · 2026-06-28T07:10:14.727717124Z · kind=evidence

# Security Audit: internal/cli/ and internal/control/

## Part A: CLI Module

### A1. Entry & Routing

**Top-level commands** (cli.go:49-114):
| Command | Handler | Description |
|---------|---------|-------------|
| `run` | `runAgent()` | Headless agent run |
| `chat`/`code` | `chatREPL()` | Interactive TUI |
| `serve` | `runServe()` | HTTP/SSE server |
| `setup` | `setupConfig()` | Wizard |
| `config` | `configCommand()` | Config management |
| `init` | `initHint()` | Point to /init |
| `acp` | `acpCommand()` | Auto commit/push |
| `mcp` | `mcpCommand()` | MCP server management |
| `doctor` | `doctorCommand()` | Diagnostics |
| `review` | `reviewCommand()` | Git review |
| `version`/`-v` | print+return 0 | Version |
| `help`/`-h` | `usage()` | Help text |

**TUI slash commands** (chat_tui.go:3066-3170):
/compact, /new, /resume, /verbose, /effort, /rewind, /tree, /branch, /switch, /mcp, /model, /skill, /skills, /hooks, /paste-image, /output-style, /theme, /language, /help, /memory, /remember, /quit, /exit

### A2. Input Processing

**Entry points:**
- `cli.go:1795` `readStdin()` — reads all from os.Stdin via `io.ReadAll`; only used when `reasonix run` has no prompt argument
- `cli.go:546` `chatREPL()` — uses bubble tea `textarea.Model` for interactive input
- `chat_tui.go:1083` — Enter key submits the textarea value

**Key flow for Enter:**
1. `strings.TrimSpace(m.input.Value())` — gets input
2. If starts with `!`: prepends to shell command, calls `m.ctrl.RunShell(cmd)`
3. If starts with `/`: checks `control.FileRefLine(line)` to detect file paths, else calls `m.runSlashCommand(line)`
4. Otherwise: `m.expandPastedBlocks(line)` expands [paste #N] labels, then `m.ctrl.SendWithRaw(sent, raw)`

**Slash command routing:** chat_tui.go:3066 — simple switch on first space-delimited word

**Input sanitization:**
- `strings.TrimSpace()` used but no ANSI escape stripping on user input
- User input is echoed as-is via `renderUserBubble()` — raw text with no escape filtering
- `pastedBlocks` expansion is simple `strings.ReplaceAll` with rendered block text

### A3. TUI Security — ANSI Escape Injection

**CRITICAL FINDING: No ANSI escape sanitization on user input.**

- `chat_tui.go:1139` — `renderUserBubble(line, m.width)` renders the user's typed input directly. If the user types or pastes text containing ANSI escape sequences (e.g., `\033[2J\033[H`), these will be executed by the terminal.
- `chat_tui.go:1083-1108` — No call to `ansi.Strip()` on user input before renderUserBubble
- The `textarea.Model` from bubble tea does not strip ANSI by default

**Mitigating factors:**
- The `charmbracelet/x/ansi` package is imported and used elsewhere (wrapTranscript, visibleWidth) — but NOT applied to user input before rendering
- bubbletea's internal rendering may partially protect against some escapes, but the `tea.Println` path (native scrollback) writes directly to the terminal

**Markdown rendering (md.go):**
- `md.go:369` — `*ast.RawHTML` is silently dropped ("// drop — rare in chat output and would print as literal escapes")
- The markdown renderer generates ANSI SGR sequences (bold, italic, dim, colors) through lipgloss — these are intentionally controlled by the renderer
- Model output goes through goldmark parsing then ANSI rendering; inline text from `*ast.Text` is written via `b.Write(v.Segment.Value(src))` — this is raw bytes from the markdown source. If the model sends ANSI escapes in its response, they pass through to terminal.

**XSS risk:** Terminal rendering, not HTML, so "XSS" isn't the right model. But ANSI escape injection (CWE-150) is real: a malicious model response or a crafted paste could send `\033[2J` (clear screen), `\033[3J` (clear scrollback), or even `\033[22;0;0t` (xterm control sequences that open URLs).

### A4. File Operations

**Resume/Replay path traversal:** 
- `resume.go:62` — `agent.LoadSession(target.Path)` — target path comes from `recentSessions()` which lists `.jsonl` files from `m.ctrl.SessionDir()` which is `config.SessionDir()`. Sessions are listed by `os.ReadDir` and filtered by `.jsonl` extension. The user cannot inject arbitrary paths via `/resume <n>` since index maps to a pre-listed file.
- `agent.LoadSession(path)` reads the path directly with `os.ReadFile(path)` — no path traversal check. But callers only pass paths from `ListSessions` or `NewSessionPath`, both constrained to the session directory.
- `cli.go:281` — `agent.IsCleanupPending(*resume)` — `*resume` is a user-provided string from `--resume` flag. If user passes a path with `../` traversal, this could read outside session dir. **Low risk** since the user runs the CLI locally but still a defense-in-depth issue.

**FileRefLine (control/refs.go:319-328):**
- `line = strings.Trim(line, "'\"")` — strips quotes
- `os.Stat(p)` — confirms file exists
- Returns `"@" + p` — no path normalization; `@../../etc/passwd` would resolve if file exists

### A5. Configuration Passing

**CLI flags to internal modules:**
- `cli.go:205` `setup()` — passes `modelName`, `maxStepsOverride`, `requireKey`, `sink` to `boot.Build()`
- `setupQuiet()` — same but redirects stderr
- `--dir` flag calls `os.Chdir(dir)` before any config reading — changes working directory
- `--dangerously-skip-permissions` / `--yolo` flag sets `ctrl.SetBypass(true)` — no validation

**Flag injection risk:**
- Uses `flag.NewFlagSet` with `flag.ContinueOnError` — standard Go flag parsing, no injection vector
- `--resume` flag value is used directly as file path without sanitization (see A4)


## Part B: Control Module

### B1. Core Responsibilities

**Controller state:** (controller.go:52-155)
- Agent runner + executor
- Permission policy + gate (for tool approval)
- Session directory, path, message history
- Checkpoint store (snapshot-based rewind)
- Branch management (fork/switching)
- Hook runner (UserPromptSubmit, Stop, Notification, SessionStart, SessionEnd)
- Memory set (project + session memories)
- Tool registry + MCP plugin host
- Background job manager
- Balance (wallet) fetcher
- Sub-agent gate for nested agent approvals

**Key methods:** Send, Submit, Cancel, Approve, RunShell, NewSession, Rewind, ForkNamed, Branch, SwitchBranch, Compact, Snapshot, Resume

### B2. Branch Management

**Branch/fork logic** (controller.go:944->forkNamed):
- Takes a snapshot of current session, copies messages up to boundary marker into new session file
- Saves branch metadata (parent, fork turn) via `agent.SaveBranchMeta`
- Switches executor and session path to the new branch
- Lock protected against concurrent running turns

**Data loss risks:**
- A failed `Snapshot()` call logs warning but continues (controller.go:964)
- Branch while running is prevented (controller.go:955)
- Rewind while running is prevented (controller.go:887)
- Rewind scope `RewindConversation` truncates messages in memory + snapshot — if snapshot fails, in-memory data is already modified
- **Minor concern:** `ForkSession` (no switch) saves metadata about parent but does NOT update the parent's branch meta — timeline is recorded but not linked bidirectionally

### B3. Attachment Handling

**Attachment flow** (attachments.go):
- `SaveImageDataURL` / `SaveAttachmentDataURL` — decodes base64 data URLs
- `SaveImageFile` / `SaveAttachmentFile` — copies files from user-provided paths
- `SaveClipboardImage` — platform-specific clipboard reads

**Path traversal protections:**
- `cleanAttachmentPath` (attachments.go:375): requires relative path, checks path is under `.reasonix/attachments/`, rejects `..` traversal, rejects symlinks in every path component via `rejectSymlinkComponents`
- `ensureAttachmentRoot` (attachments.go:418): validates attachment directory is not a symlink
- `createAttachmentFile` generates random filenames within `.reasonix/attachments/`
- `safeAttachmentExt` regex: `^\.[a-z0-9]{1,12}$` — restricts extensions to safe characters

**Type confusion protections:**
- `detectedImageMime` checks magic bytes before trusting declared MIME type
- Size limits: 10MB for images, 25MB for files
- Symlink checks on `SaveImageFile` / `SaveAttachmentFile` — rejects symlinks
- TOCTOU protection: `os.SameFile` checks before and after read

**Clipboard image commands** (attachments.go:231-268):
- Windows: runs PowerShell script to read clipboard — hardcoded script, no injection
- Linux: runs `wl-paste` or `xclip` — fixed arguments
- macOS: runs `osascript` with fmt.Printf'd absolute path — **potential injection** in `abs` variable? The path comes from `filepath.Abs(rel)` where `rel` is a generated name from `createAttachmentFile`, so safe. But if `createAttachmentFile` returned a path with shell metacharacters... `createAttachmentFile` runs `filepath.Join` with `.reasonix/attachments/` + safe random name, so safe.

### B4. Reference Handling

**@reference resolution** (refs.go):
1. `parseRefTokens` extracts tokens following `@` via regexp `@([^\s]+)`
2. `detectRefsMode` classifies each token: MCP resource, image attachment, file, or ignore
3. `resolveRefs` resolves file refs via `readFileRef`

**Path traversal protections in file refs:**
- `resolveAbsRef` (refs.go:689): with `baseDir`, computes relative path and rejects if `!filepath.IsLocal(rel)`
- `workspaceRefPath` (refs.go:406): opens `os.OpenRoot(absBase)` and uses `root.Stat(rel)` — sandboxed under workspace root
- `readFileRef` (refs.go:497): uses sandboxed root when baseDir is set; falls back to unscoped for CLI
- `isSafeBareRefName` (refs.go:305): rejects `..`, `.`, paths with separators, checks `filepath.IsLocal`
- `FileRefLine` (refs.go:319): no `baseDir` sandbox — used from TUI where it trusts the local file system

**MCP resource resolution:**
- `c.host.ReadResource(ctx, r.server, r.uri)` — delegates to plugin host
- The URI is user-controlled (comes from `@server:uri`). Server name is checked against connected servers. URI is passed to the MCP server's `resources/read` — depends on MCP server implementation for safety.

**@reference injection into model context:**
- `readFileRef` reads up to 64KB of file content and injects it into the model's context wrapped in `<file path="...">...</file>` tags
- Directory listings limited to 100 entries
- PDF extraction runs external commands (`pdftotext` or Python) with the file path as argument — **potential command injection** if the reference path contains shell metacharacters (see B5)

### B5. Slash Command Execution & Injection

**Submit flow** (controller.go:430):
1. `!cmd` → `RunShell` — executes shell command with user input
2. `/compact` → Compact
3. `/new` → NewSession
4. `/mcp_` → MCPPrompt  
5. Other `/` commands → various handlers
6. Otherwise → `runRefTurn` → ResolveRefs + runTurnWithRaw

**Shell command execution** (controller.go:585-651):
- `shell.ResolveShell().Argv(command)` splits command for exec
- `exec.CommandContext(ctx, argv[0], argv[1:]...)` — runs without shell interpretation
- `sh.Argv(command)` for bash returns `["bash", "-c", command]` — **user input IS interpreted by bash** for the bash shell path
- For PowerShell: `["powershell", "-NoProfile", "-NonInteractive", "-Command", psUTF8Prologue+command]` — also passed as argument
- Both paths execute user-controlled strings via shell interpretation: this is an intentional feature ("!<cmd>"), not injection. The user running the CLI can already run shell commands.
- **Risk level:** Acceptance. The `!` prefix is explicitly designed for this.

**PDF extraction command execution** (refs.go:731-755):
- `runPDFTextCommand` calls `exec.Command(name, args...)` where `args` includes the file path
- File path comes from user's `@file.pdf` reference — could contain spaces or special chars
- Arguments are passed as separate argv elements (not shell-joined), so command injection via the path is **not possible** for pdftotext/python
- **But** the `python -c` path includes the file path as an argument to `python -c script` — the path is part of `sys.argv` in the Python script, not executed as code, so safe from injection

**MCP edit config command** (mcp_manager_actions.go:328):
- `exec.Command("sh", "-lc", shellQuote(editor)+" "+shellQuote(path))` — VISUAL/EDITOR env vars passed through shell with `shellQuote`
- `shellQuote` wraps in single quotes and escapes single quotes — safe but depends on sh interpretation
- **The `editor` variable comes from environment variables** (VISUAL, EDITOR). If an attacker controls the environment, they could inject commands. But this is standard UNIX behavior.

### B6. Permission Integration & Bypass

**Permission architecture:**
1. `config.Permissions` defines mode (ask/allow/deny), allow/ask/deny rules
2. `permission.New(mode, allow, ask, deny)` creates a `Policy`
3. `permission.NewGate(policy, approver)` wraps policy with interactive approver
4. Controller stores `policy` and creates `gate` via `EnableInteractiveApproval()`
5. Agent uses `gate.Decide(toolName, readOnly, args)` to check permissions

**Bypass paths:**

1. **`--dangerously-skip-permissions` / `--yolo`** (cli.go:553):
   - Sets `ctrl.SetBypass(true)` → `c.bypass = true`
   - `gateApprover.Approve()` checks `c.bypass` first — auto-allows everything
   - Deny rules still apply (checked before gate in Policy.Decide)
   - **No warning/confirmation** when user types `!/--yolo` flag — documented as dangerous

2. **DisableInteractiveApproval:**
   - Without `EnableInteractiveApproval()`, the executor has no `Gate` — all tool calls proceed unchecked
   - Sub-agent controllers get `subAgentGate` which is separately configured
   - Desktop/headless paths might skip setting up a gate

3. **Ask tool bypass:**
   - `Ask()` explicitly NOT bypassed in YOLO mode (controller.go:751-753)
   - But `gateApprover.Approve` IS bypassed — only writer tools are affected
   - Both paths are correctly distinguished

4. **No race window in approval:**
   - `promptMu` serialises approval prompts
   - Re-check of bypass/grant under lock after acquiring promptMu
   - Reply channels are buffered (size 1) to prevent blocking

**Permission rule evaluation** (permission.go):
- Subject extraction from JSON args uses priority-ordered keys
- `matchGlob` is a custom matcher — `*` matches any run (including `/`)
- Remembered rules use `Literal=true` mode — exact match only, no glob expansion
- Category support ("Edit" = [move_file, write_file]) works correctly

## note #3 · 2026-06-27T03:37:28.091370993Z · kind=evidence

# chunkRing 代码审计报告

## 总览
`chunkRing` 是 OpenAI 和 Anthropic 两个 provider 中完全相同的 ring buffer 实现（代码重复拷贝），用于解耦 HTTP SSE 读取 goroutine 与消费者之间的背压。容量 `cap=64`，策略为「丢旧留新」（drop-oldest）。

---

## 发现

### P0 — close() 在 buffer 非空且无消费者读取时死锁

**问题描述：** `close()` 在关闭 stopCh 后等待 `<-cr.done`，但 drain goroutine 的 `drainAll()` 在尝试发送缓冲区内剩余 chunk 到 `out` 时，如果 `out` 满且 ctx 未被取消，则永久阻塞。`drainAll()` 内部仅检查 `<-cr.ctx.Done()`，不检查 `stopCh`。这导致 `close()` 永远无法返回。

**代码位置：** 
- `openai.go:327-340` (`drainAll`) — 内层 select 只检查 `ctx.Done()`
- `openai.go:370-381` (`close`) — 等待 `<-cr.done`，但 drain 被阻塞
- anthropic 相同位置

**触发条件（生产环境）：**
1. 消费者因 ChunkError 提前退出读取循环（agent.go:887）
2. 此时 buffer 中仍有未被 drain 的 chunk（例如 streamWithReconnect 发送的第 2 个 ChunkError）
3. ctx 未被取消
4. `close()` 被 defer 调用 → 死锁

**复现：** 我编写的压力测试 `TestChunkRingCloseRace` 在 30 秒后 timeout，stack trace 显示 `close()` 在 `<-cr.done` 处阻塞，而 drain goroutine 在 `drainAll()` 的 `cr.out <- chunk` 处阻塞。

**修复建议：** `drainAll()` 内层 select 应增加 `<-cr.stopCh` 分支，或给 `close()` 增加超时：

```go
// 方案 A：在 drainAll 内层 select 增加 stopCh
select {
case cr.out <- chunk:
case <-cr.ctx.Done():
    return
case <-cr.stopCh:
    return
}
```

```go
// 方案 B：close() 加超时保护（备选方案）
func (cr *chunkRing) close() {
    close(cr.stopCh)
    select {
    case <-cr.done:
    case <-time.After(5 * time.Second):
        // timeout: drain goroutine stuck, force exit
    }
    close(cr.out)
}
```

**严重性：P0** — 一旦触发，goroutine 永久泄漏，`close()` 永不返回。

---

### P1 — drainAll 在 ctx 取消时不保证 buffer 排空

**问题描述：** `drain()` 在收到 `<-cr.ctx.Done()` 后调用 `drainAll()`，而 `drainAll()` 内部也会因 `ctx.Done()` 提前返回，导致 buffer 中剩余 chunk 静默丢弃。注释声称「drain goroutine always drains remaining chunks before exiting」，但 ctx 取消路径下该保证不成立。

**代码位置：** `openai.go:348-365`、anthropic 对应位置

**影响：** 当用户取消请求（Ctrl+C）时，buffer 中最多 63 个 chunk（文本/推理/工具调用）可能丢失。工具调用 chunk 丢失会导致工具结果丢失（输出损坏），ChunkDone 丢失则消费者侧 channel 正常关闭（因为 close() 会 close(out)），但中间文本 chunk 丢失导致输出不完整。

**修复建议：** ctx 取消路径下仍应尝试发送剩余 chunk，直到 buffer 排空或超时：

```go
case <-cr.ctx.Done():
    // 尽力排空，不依赖 ctx.Done() 退出内层循环
    cr.drainAllWithTimeout(100 * time.Millisecond)
    return
```

**严重性：P1** — 仅在用户取消时触发，但可能导致输出不完整。

---

### P1 — 代码重复：两个 provider 维护完全相同的 chunkRing

**问题描述：** `openai.go:255-381` 和 `anthropic.go:490-620` 是完全相同的 chunkRing 实现（仅注释措辞有细微差异）。任何 bug 修复需要在两处同步修改。

**代码位置：** 
- `openai.go:255-381`
- `anthropic.go:490-620`

**修复建议：** 将 chunkRing 提取到公共包（如 `internal/provider/chunkring.go`），两个 provider 共享同一份代码。

**严重性：P1** — 不直接影响正确性，但严重降低可维护性。

---

### P1 — 无单元测试覆盖 chunkRing

**问题描述：** chunkRing 没有任何直接单元测试。现有测试通过集成测试（`readStream` + mock HTTP server）间接覆盖，但 buffer 满时丢 chunk、close 时排空、并发顺序等边界条件未被测试。

**代码位置：** 整个 chunkRing 逻辑无测试文件。

**修复建议：** 增加以下测试：
1. `TestChunkRingOrder` — 并发 send 下 FIFO 顺序
2. `TestChunkRingBufferFull` — 丢旧留新策略验证
3. `TestChunkRingCloseDeadlock` — close 时 buffer 非空 + 无消费者
4. `TestChunkRingContextCancel` — ctx 取消时 drain 正确退出
5. `TestChunkRingConcurrent` — 多 send 并发安全

**严重性：P1** — 无测试覆盖的关键基础设施组件。

---

### P2 — send() 在 stopCh 关闭后有短暂竞态窗口

**问题描述：** `send()` 在检查 `stopCh` 之后、获取 mutex 之前存在竞态窗口。如果 `close()` 在此窗口内关闭 `stopCh`，chunk 会被添加到 buffer，但 drain goroutine 已退出（或即将退出），导致该 chunk 永不被消费。`send()` 返回 `true`（调用者认为已接受），但 chunk 实际丢失。

**代码位置：** `openai.go:291-321`（send 函数）

```go
select {
case <-cr.stopCh:  // 检查 stopCh
    return false
default:
}                     // ← 竞态窗口：此时 close() 关闭 stopCh
cr.mu.Lock()          // ← 锁获取
cr.buf = append(cr.buf, chunk)  // chunk 入队但无法被消费
cr.mu.Unlock()
```

**影响：** 调用者基于 `true` 返回值认为 chunk 已成功入队，实际可能丢失。在 `streamWithReconnect` 中，ChunkError 可能丢失（不过此时上下文已经结束，影响有限）。

**修复建议：** 在 mutex 保护下重新检查 stopCh：

```go
cr.mu.Lock()
select {
case <-cr.stopCh:
    cr.mu.Unlock()
    return false
default:
}
if len(cr.buf) >= cr.cap {
    cr.buf = cr.buf[1:]
}
cr.buf = append(cr.buf, chunk)
cr.mu.Unlock()
```

**严重性：P2** — 仅在并发 close + send 时触发，且丢失的通常是冗余的 ChunkError。

---

### P2 — 容量 64 的合理性未经验证

**问题描述：** `cap=64` 的选择没有注释说明依据。在特定场景下（消费者处理工具调用时停顿 + 模型快速生成大量短 chunk），64 个槽位可能不足以吸收背压，导致文本 chunk 丢失，输出不完整。

**代码位置：** `openai.go:213`、`anthropic.go:163`

**影响：** 文本 chunk 丢失 → 输出句子断裂。工具调用 chunk 丢失（ChunkToolCall、ChunkToolCallStart）→ 工具调用失败或前端 UI 异常。

**修复建议：** 
1. 注释说明 64 的选择依据
2. 考虑增大为 256 或 1024（chunk 结构体很小，~80 bytes，64*80=5KB，256*80=20KB，均可接受）
3. 增加监控：如果 buffer 满导致丢 chunk，记录日志以便调优

**严重性：P2** — 需要实际数据来确认是否触发。

---

### P2 — slice 操作可能阻止 GC

**问题描述：** `cr.buf = cr.buf[1:]` 通过 re-slice 丢弃最旧 chunk。Go 的 slice 操作保留对底层数组的引用，被丢弃的 `buf[0]` 无法被 GC 回收，直到整个底层数组被回收。长期运行的流可能导致内存累积。

**代码位置：** `openai.go:309-312`、`anthropic.go:546-549`

**修复建议：** 在删除后显式清空引用：

```go
if len(cr.buf) >= cr.cap {
    cr.buf[0] = provider.Chunk{}  // allow GC
    cr.buf = cr.buf[1:]
}
```

**严重性：P2** — 实际内存泄漏量很小（每个 chunk ~80 bytes），累积最多 ~5KB。

---

### P2 — openai 的 streamWithReconnect 可能发送重复 ChunkError

**问题描述：** 当 `readStream` 返回非连接重置错误时，`streamWithReconnect` 发送 `ChunkError`。但某些错误路径下（如 `sawDone=false` 且 `emitted=true`），`readStream` 返回的 `StreamInterruptedError` 可能被重复包装并发送为两个 ChunkError（一个来自 `streamWithReconnect` 的非连接重置路径，但此路径实际仅发送一个）。

**代码位置：** `openai.go:234-236`

```go
if !provider.IsConnReset(err) {
    cr.send(ctx, provider.Chunk{Type: provider.ChunkError, Err: err})
    return
}
```

**影响：** 消费者收到两个 ChunkError（第一个来自 readStream 内部？实际上 openai 的 readStream 不发送 ChunkError，只有返回错误）。但 `streamWithReconnect` 在 `emitted=true` 路径下（行 238-240）发送的 `StreamInterruptedError` 被包装了两层，消费者侧可能混淆。

**修复建议：** 统一错误发送逻辑，避免 readStream 和 streamWithReconnect 双发。

**严重性：P2** — 消费者能正确处理多个 ChunkError。

---

### P3 — wakeCh 缓冲为 1 可能导致 drain 延迟

**问题描述：** `wakeCh` 容量为 1，当 drain goroutine 正在 drainAll 时，新的 wake 信号被丢弃。这不会导致问题（drainAll 会持续排空直到 buffer 空），但如果 drainAll 中途返回（ctx 取消），而新 chunk 在 drain 返回 select 之前到达，则 chunk 可能延迟被处理。

**代码位置：** `openai.go:279`、`anthropic.go:516`

**影响：** 在高吞吐场景下可能导致 chunk 短暂延迟，但不会丢失或乱序。

**严重性：P3** — 仅影响延迟，不影响正确性。

---

## 总体评价

`chunkRing` 的设计思路正确（解耦 HTTP 读取和消费者处理），基本功能（FIFO、丢旧留新、并发安全）实现良好。`-race` 测试无数据竞争。

**最严重的风险是 P0 死锁问题**，虽然生产环境触发条件较苛刻，但一旦触发会导致 goroutine 永久泄漏和 `close()` 永不返回。**强烈建议优先修复。**

次要问题包括 ctx 取消时 buffer 未排空、代码重复、无单元测试覆盖、以及容量选择缺少依据。

## note #2 · 2026-06-24T17:09:17.058130004Z · kind=summary

# 缩编 DefaultMainAgentAllowed 完成

## 变更内容

### 1. `internal/agent/agent.go` — 默认白名单从30工具缩减到7核心工具

**前：** 30 个工具（remember, forget, install_skill, explore, research, review, security_review, list_scheduled_tasks, list_sessions, read_session, read_file, grep, glob, ls, lsp_definition, lsp_hover, lsp_references, lsp_diagnostics, bash_output, ctx_read, ctx_search, ctx_index, ctx_run 等）

**后：** 7 个最小集合 — `task`, `ask`, `note`, `audit_finish`, `read_skill`, `run_skill`, `slash_command`

**语义变化：**
- 原：`MainAgentAllowed == nil` → 不做限制，主代理可用全部注册工具
- 新：`MainAgentAllowed == nil` → 使用 `DefaultMainAgentAllowed`（7工具最小集）
- 自定义 map 可进一步收窄或放宽（`reasonix.toml` 的 `permissions.main_agent_allowed`）

### 2. `internal/config/render.go` — 新增 main_agent_allowed 配置渲染

- 在 `[permissions]` 区块中添加了 `main_agent_allowed` 的注释说明和渲染
- 默认状态下显示为注释的示例值，用户取消注释即可启用自定义白名单
- 用户只需维护 `reasonix.toml` 一份配置文件

### 3. 兼容性说明

- 子代理不受 `MainAgentAllowed` 影响（它们走 skill 的 `allowed-tools` 机制）
- Ship CI/CD skill 作为子代理（runAs: subagent），有自己的 `allowed-tools` 列表，完全不受影响
- 用户可通过 `reasonix.toml` 的 `permissions.main_agent_allowed` 自定义白名单

## note #1 · 2026-06-24T10:53:37.784051918Z · kind=evidence

# Audit: MCP 路由改动

## 检查文件
- `/root/reasonix/internal/tool/tool.go` (SplitMCPName, RegisterMCPPrefixes, RemovePrefix)
- `/root/reasonix/internal/plugin/plugin.go` (ToolPrefix, ServerPrefixes, Host.Remove, Host.Add, Start)

---

## 1. `SplitMCPName` (tool.go:177–202)

### 1a. 不以 `mcp_` 开头时返回 ok=false
- **结果**: ✅ 通过
- **代码**: 第178-180行
  ```go
  if !strings.HasPrefix(name, MCPNamePrefix) {
      return "", "", false
  }
  ```
- 正确：当 name 不以 `"mcp_"` 开头时立即返回 `false`。

### 1b. `mcpPrefixes` 为 nil/空时安全处理（不会 panic）
- **结果**: ✅ 通过
- **代码**: 第181-185行
  ```go
  raw := mcpPrefixes.Load()
  prefixes, ok := raw.([]string)
  if !ok || len(prefixes) == 0 {
      return "", "", false
  }
  ```
- `atomic.Value` 在从未 Store 时 Load() 返回 `nil`。`nil.([]string)` 类型断言成功（nil 是有效的 nil `[]string`），`len(prefixes) == 0` 对 nil 也成立 => 不会 panic。

### 1c. 最长前缀匹配逻辑正确
- **结果**: ✅ 通过
- **代码**: 第187-195行
  ```go
  best := ""
  for _, p := range prefixes {
      if strings.HasPrefix(name, p) && len(p) > len(best) {
          best = p
      }
  }
  if best == "" {
      return "", "", false
  }
  ```
- 遍历所有前缀，用 `len(p) > len(best)` 比较保留最长匹配。当无匹配时返回 false。逻辑正确。

### 1d. server 名提取 `prefix[4:len(prefix)-1]`
- **结果**: ✅ 通过
- **代码**: 第200行
  ```go
  server = best[len(MCPNamePrefix) : len(best)-1]
  ```
- `MCPNamePrefix = "mcp_"` 长度为4。`len(best)-1` 去掉尾部的 `_`。例如 `"mcp_jina_"` → `best[4:9]` = `"jina"`，`"mcp_codegraph_"` → `best[4:14]` = `"codegraph"`。**假设所有前缀以 `_` 结尾**（由 `ToolPrefix` 保证，见下），正确。
- ⚠ **注意事项**：如果某个前缀不以 `_` 结尾，`len(best)-1` 会切掉最后一个字符而非尾部分隔符。但前缀的唯一来源是 `ToolPrefix()`（后文验证），它固定返回 `"mcp_" + normalizeName(server) + "_"`，因此安全。

### 1e. 并发安全性（`atomic.Value`）
- **结果**: ✅ 通过
- **代码**: 
  - 声明 `var mcpPrefixes atomic.Value` (第163行)
  - 写入: `mcpPrefixes.Store(prefixes)` (第170行)
  - 读取: `mcpPrefixes.Load()` (第181行)
- `atomic.Value` 保证 Store/Load 的 happens-before 语义。只有一个写入者（`RegisterMCPPrefixes`）+ 多个读取者（`SplitMCPName`），使用模式正确。

---

## 2. `ToolPrefix` (plugin.go:957–959)

- **结果**: ✅ 通过
- **代码**:
  ```go
  func ToolPrefix(server string) string {
      return "mcp_" + normalizeName(server) + "_"
  }
  ```
- 返回格式为 `"mcp_<server>_"`，符合文档说明。

---

## 3. `RegisterMCPPrefixes` 的调用点（3处）

### 3a. Start 函数末尾 (plugin.go:354)
- **结果**: ✅ 通过
- **上下文**: 第325-354行。所有 `specs` 的并行启动完成后，每个成功的结果将 client append 到 `h.clients`（第346行），然后调用 `tool.RegisterMCPPrefixes(h.ServerPrefixes())`。
- **时机正确**: plugin 列表变更（所有初始 plugin 连接完成）后立即调用。

### 3b. Host.Add 内部 (plugin.go:658)
- **结果**: ✅ 通过
- **上下文**: 第635-658行。新 client 连接成功、tools/list 完成、`h.clients = append(h.clients, c)`（第655行）之后调用。
- **时机正确**: 热添加新 server 后立即调用。
- **前缀收集正确**: `h.ServerPrefixes()` 遍历 `h.clients` 并对每个 client 调用 `ToolPrefix(c.name)`。

### 3c. Host.Remove 内部 (plugin.go:802)
- **结果**: ✅ 通过
- **上下文**: 第769-802行。从 `h.clients` 中移除目标 server、清理 prompts/resources/failures 之后、调用 `removed.close()` 之前调用。
- **时机正确**: 移除 server 后立即更新前缀列表，防止 `SplitMCPName` 继续匹配已断开的 server。

---

## 4. `Host.Remove` 返回格式 (plugin.go:806)

- **结果**: ✅ 通过
- **代码**:
  ```go
  return "mcp_" + normalizeName(name) + "_", true
  ```
- 返回的 `toolPrefix` 格式为 `"mcp_<server>_"`，与 `ToolPrefix(name)` 结果一致，可供调用者直接传给 `reg.RemovePrefix(prefix)`。

### 4a. 调用者验证
- **controller.go:1629-1632** (`RemoveMCPServer`): `c.host.Remove(name)` 返回的 prefix 正确传给 `c.reg.RemovePrefix(prefix)` ✅
- **controller.go:1662-1665** (`DisconnectMCPServer`): 同上 ✅
- **boot.go:552-553** (disconnect closure): 同上 ✅
- **boot.go:562-563** (`OnDisconnect`): 同上 ✅

---

## 额外发现

### 问题 1: `Host.Remove` 的返回值与 `ToolPrefix` 重复逻辑
- **文件**: `plugin.go:806`
- **严重性**: ⚠️ 小建议
- **说明**: `Remove` 的 return 语句手写了 `"mcp_" + normalizeName(name) + "_"`，这等价于调用 `ToolPrefix(name)`。如果 `ToolPrefix` 的实现逻辑未来变化（例如添加了合法性校验），这里可能不同步。
- **建议**: 改为 `return ToolPrefix(name), true` 以消除重复。

### 问题 2: Auto-restart goroutine 忽略了 `Remove` 返回的 prefix（可能不是 bug，但需留意）
- **文件**: `plugin.go:745`
- **严重性**: ℹ️ 观察项
- **说明**: Auto-restart goroutine 内部调用 `h.Remove(s.Name)` 时未使用返回的 prefix 去清理工具注册表。随后 `h.Add(restartCtx, s)` 返回的 tools 也未注册到全局 `Registry`。但该 goroutine 在 `Add` 内部，而 `Add` 的调用者负责管理注册表，重启后需要重新 Add tools 到 reg。
- **建议**: 确认 auto-restart 场景下工具注册表的清理和重建是否由调用链路保证。

---

## 总结

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 1a. 非 mcp_ 前缀返回 false | ✅ 通过 | |
| 1b. nil/空 prefixes 安全 | ✅ 通过 | |
| 1c. 最长前缀匹配 | ✅ 通过 | |
| 1d. server 名提取 | ✅ 通过 | 依赖前缀以 `_` 结尾，由 `ToolPrefix` 保证 |
| 1e. 并发安全 | ✅ 通过 | |
| 2. ToolPrefix 返回格式 | ✅ 通过 | `"mcp_<server>_"` |
| 3a. Start 中调用 | ✅ 通过 | line 354 |
| 3b. Add 中调用 | ✅ 通过 | line 658 |
| 3c. Remove 中调用 | ✅ 通过 | line 802 |
| 4. Remove 返回格式 | ✅ 通过 | `"mcp_" + normalizeName(name) + "_"` |
